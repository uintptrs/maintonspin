const STATUS = Object.freeze({
  INSUFFICIENT_BALANCE:
    "Your wallet has a low transaction volume, which means you are not eligible to receive rewards.",
  TX_LIST_READY: "Transaction list is ready !",
});

export default STATUS;
